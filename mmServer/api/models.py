""" mmServer.api.models

    This module defines the database models used by the mmServer.api
    application.

    Note that this module is deliberately empty, as the mmServer.api
    application does not define any database models of its own.
"""

# This module is deliberately empty.

